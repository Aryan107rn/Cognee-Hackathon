// Cognee API Service
export const cogneeApi = {
  initialize: () => {
    // Initialize Cognee
  },
  request: async (endpoint, method = 'GET', data = null) => {
    // Make request to Cognee API
  },
};

export default cogneeApi;
