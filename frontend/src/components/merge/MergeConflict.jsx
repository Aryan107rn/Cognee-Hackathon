import React from 'react';

export const MergeConflict = ({ conflict }) => {
  return (
    <div className="merge-conflict">
      {/* Merge conflict content */}
    </div>
  );
};

export default MergeConflict;
