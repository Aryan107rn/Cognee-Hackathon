// Workspace Store
export const workspaceStore = {
  state: {
    workspace: null,
    workspaces: [],
  },
  actions: {
    setWorkspace: (workspace) => {
      // Set workspace
    },
    getWorkspace: () => {
      // Get workspace
    },
  },
};

export default workspaceStore;
