// Chat Store
export const chatStore = {
  state: {
    messages: [],
    currentChat: null,
  },
  actions: {
    addMessage: (message) => {
      // Add message
    },
    getMessages: () => {
      // Get messages
    },
  },
};

export default chatStore;
